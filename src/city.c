#include <stdlib.h>


typedef struct vehicle{
	int size;
	int speed;
	int trips;
	steet * destination;
	char * color;
	char * model;
};


typedef struct steet{
	int weight;
	vehicle * spot=calloc(5,sizeof(vehicle));
	steet * AdStreets=calloc(4,sizeof(steet));
};

typedef struct city{
	
}; 



 init city;
