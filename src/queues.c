
#include "vehicle.h"
#include <stdio.h>
#include <stdlib.h>
#include <city.h>


void Push(pqueu* queue,vehicle *pcar, int priority){
	// struct pnode *node = (struct pnode *)calloc(1, sizeof(struct pnode));
	// node->priority = priority;
	// if (queue->size==0) {
	// 	queue->first=&node;
	// 	queue->last=&node;
	// 	queue->size=queue->size+1;
	// }
	// if(queue->maxsize>queue->size){
	// 	node  = (struct pnode *)calloc(1, sizeof(struct pnode));
	// 	node->priority=priority;
	// 	node->car=pcar;
	// 	//while()

	// }
}

vehicle Pop(pqueu* queue){
	// vehicle temp = (*queue).first;
	// if (queue->size!=0){
	// 	queue->first=queue->first->next;
	// 	queue->size=queue->size-1;
	// 	free(queue);
	// }
	// return temp!=NULL? temp : NULL;	
}

